# This is a basic workflow to help you get started with Actions

name: Integración y entrega continua

# Controls when the workflow will run
on:
  # Triggers the workflow on push or pull request events but only for the "main" branch
  push:
    branches: [ "main" ]
 
  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  build:
    env:
      nombres: "Ruth Anai"
      apellidos: "Garcia Siancas"
      edad: 29

      user: ${{ secrets.USER }}
      password: ${{ secrets.PASSWORD }}

    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4

      # Mi presentación
      - name: presentación
        run: |
          echo "Impresión de mi presentación:"
          echo "Mi nombre es: ${{ env.nombres }}"
          echo "Mi apellido es: ${{ env.apellidos }}"
          echo "Mi edad es: ${{ env.edad }}"

       # Mis datos generales
      - name: datos generales
        run: |
          echo "Impresión de mis datos generales:"
          echo "carrera de ingeniería de sistemas"


       # Impresión de mi variables secretas
      - name: variables de entorne secretas
        run: |
          echo "Mi usuario es: ${{ env.user }}"
          echo "Mi contraseña es: ${{ env.password }}"

       # Mis variables abiertas
      - name: variables abiertas
        run: |
          echo "La versión de mi proyecto: ${{ vars.VersionDeMiProyecto}}"
          
